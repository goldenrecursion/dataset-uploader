from django.db import models


# Create your models here.

#Update: CustomerUser model in Users app
#class User(models.Model):
#(extend the User model instance, for instance with a field for the Godel JWT)

#class Entity(models.Model):
#(atomic unit in the golden protocol)

#class Triple(models.Model):
#(group of entites and associated predicates)

#class Chart(models.Model): 
#(group of triples uploaded together, with their statuses)

